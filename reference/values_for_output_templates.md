# Values for glue output templates; strip extensions when cli.strip_ext is set

Values for glue output templates; strip extensions when cli.strip_ext is
set

## Usage

``` r
values_for_output_templates(spec, values)
```
