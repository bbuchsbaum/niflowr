# Stop unless two images share dimensions and voxel-to-world transform

Stop unless two images share dimensions and voxel-to-world transform

## Usage

``` r
ni_check_same_grid(a, b, what = "Images")
```
