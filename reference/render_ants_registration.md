# Render a staged antsRegistration command (renderer: ants_registration_staged)

Render a staged antsRegistration command (renderer:
ants_registration_staged)

## Usage

``` r
render_ants_registration(call)
```
