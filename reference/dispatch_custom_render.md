# Dispatch a spec's declared custom renderer

Dispatch a spec's declared custom renderer

## Usage

``` r
dispatch_custom_render(name, call)
```

## Arguments

- name:

  Renderer name from `spec$render`.

- call:

  An `ni_call` object.

## Value

A list with `command`, `args`, `stdout`, `stderr`.
