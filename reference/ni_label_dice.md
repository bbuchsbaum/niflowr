# Per-label Dice for two label images on the same grid (label 0 is background)

Per-label Dice for two label images on the same grid (label 0 is
background)

## Usage

``` r
ni_label_dice(fixed, moving)
```
