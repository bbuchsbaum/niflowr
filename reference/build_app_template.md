# Build the app.R template with optional tool examples

Build the app.R template with optional tool examples

## Usage

``` r
build_app_template(app_name, path, tools = NULL)
```

## Arguments

- app_name:

  Name of the app

- path:

  Path to the app directory

- tools:

  Optional character vector of spec IDs

## Value

Character vector of template lines
