# Render a value with sprintf, splitting the flag from the value but keeping the value as one token (for joined list values that may contain spaces)

Render a value with sprintf, splitting the flag from the value but
keeping the value as one token (for joined list values that may contain
spaces)

## Usage

``` r
render_nosplit(value, argstr)
```
