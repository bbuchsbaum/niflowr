# Render a high-level transform-build command (renderer: ants_transform_build)

Render a high-level transform-build command (renderer:
ants_transform_build)

## Usage

``` r
render_ants_transform_build(call)
```
