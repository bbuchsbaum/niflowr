# Resolve transform metadata and domain images from an ni_result

Resolve transform metadata and domain images from an ni_result

## Usage

``` r
ni_transform_context(
  result,
  output_name = NULL,
  type = NULL,
  source_image = NULL,
  target_image = NULL
)
```
