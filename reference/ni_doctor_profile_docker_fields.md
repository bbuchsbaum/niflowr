# Validate optional per-profile Docker field shapes without aborting.

Validate optional per-profile Docker field shapes without aborting.

## Usage

``` r
ni_doctor_profile_docker_fields(profile, profile_name)
```
