# Assemble the final argument vector from global + ordered stage groups

Assemble the final argument vector from global + ordered stage groups

## Usage

``` r
render_ants_registration_core(command, pre_stage, stages, post_stage)
```
