# Whether an FSL input should strip known extensions before CLI rendering

Whether an FSL input should strip known extensions before CLI rendering

## Usage

``` r
fsl_needs_strip_ext(name, argstr)
```
