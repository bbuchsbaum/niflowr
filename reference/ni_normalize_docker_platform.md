# Normalize optional per-profile Docker `--platform` override.

Normalize optional per-profile Docker `--platform` override.

## Usage

``` r
ni_normalize_docker_platform(platform, profile = NULL)
```
