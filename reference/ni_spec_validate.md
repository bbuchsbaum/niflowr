# Validate a parsed spec list against the schema

Validate a parsed spec list against the schema

## Usage

``` r
ni_spec_validate(spec, source = "spec")
```

## Arguments

- spec:

  A parsed spec (list).

- source:

  Description of the source (for error messages).

## Value

Invisible `TRUE` on success; raises an error on failure.
