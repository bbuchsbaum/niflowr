# Write a provenance JSON sidecar

Write a provenance JSON sidecar

## Usage

``` r
ni_provenance_write(result, path = NULL)
```

## Arguments

- result:

  An `ni_result` object.

- path:

  Output path for the JSON sidecar. If `NULL`, derived from the primary
  output path.

## Value

The path written (invisibly).
