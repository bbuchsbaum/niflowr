# Read niflowr project config from YAML

Read niflowr project config from YAML

## Usage

``` r
ni_config_read(path = "niflowr.yml")
```

## Arguments

- path:

  Path to a `niflowr.yml` file.

## Value

Parsed configuration list.
