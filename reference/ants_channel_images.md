# Image for metric `j` of a stage: images pair with the metrics within a stage (as in nipype), with a single image shared by every metric

Image for metric `j` of a stage: images pair with the metrics within a
stage (as in nipype), with a single image shared by every metric

## Usage

``` r
ants_channel_images(x, name, max_metrics)
```
