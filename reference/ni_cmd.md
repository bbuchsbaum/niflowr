# Get the command + args that would be executed

Get the command + args that would be executed

## Usage

``` r
ni_cmd(call)
```

## Arguments

- call:

  An `ni_call` object.

## Value

A list with `command`, `args`, `wd`, `env`, `stdout`, `stderr`.
