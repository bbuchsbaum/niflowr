{
  "spec_version": "0.1.0",
  "id": "ants.apply_transforms",
  "title": "ANTS ApplyTransforms",
  "command": "antsApplyTransforms",
  "inputs": {
    "args": {
      "type": "string",
      "desc": "Additional parameters to the command",
      "cli": {
        "argstr": "%s"
      }
    },
    "default_value": {
      "type": "double",
      "default": 0,
      "cli": {
        "argstr": "--default-value %g"
      }
    },
    "dimension": {
      "type": "enum",
      "desc": "This option forces the image to be treated as a specified-dimensional image. If not specified, antsWarp tries to infer the dimensionality from the input image.",
      "choices": [
        2,
        3,
        4
      ],
      "cli": {
        "argstr": "--dimensionality %d"
      }
    },
    "float": {
      "type": "bool",
      "desc": "Use float instead of double for computations.",
      "default": false,
      "cli": {
        "argstr": "--float %d"
      }
    },
    "input_image": {
      "type": "file",
      "desc": "Image to transform. Not needed when print_out_composite_warp_file writes the composed displacement field instead.",
      "required": false,
      "cli": {
        "argstr": "--input %s"
      }
    },
    "input_image_type": {
      "type": "enum",
      "desc": "Option specifying the input image type of scalar (default), vector, tensor, or time series.",
      "choices": [
        0,
        1,
        2,
        3
      ],
      "cli": {
        "argstr": "--input-image-type %d"
      }
    },
    "interpolation": {
      "type": "enum",
      "default": "Linear",
      "choices": [
        "Linear",
        "NearestNeighbor",
        "CosineWindowedSinc",
        "WelchWindowedSinc",
        "HammingWindowedSinc",
        "LanczosWindowedSinc",
        "MultiLabel",
        "Gaussian",
        "BSpline",
        "GenericLabel"
      ],
      "cli": {
        "argstr": "%s"
      },
      "desc": "Interpolation for the output image; use NearestNeighbor or GenericLabel for masks and label images."
    },
    "output_image": {
      "type": "file",
      "desc": "Output image, or the composed displacement field when print_out_composite_warp_file is TRUE.",
      "cli": {
        "argstr": "--output %s"
      },
      "role": "output",
      "required": true
    },
    "reference_image": {
      "type": "file",
      "desc": "reference image space that you wish to warp INTO",
      "required": true,
      "cli": {
        "argstr": "--reference-image %s"
      }
    },
    "transforms": {
      "type": "list",
      "desc": "Transform files, applied in reverse order (the last is applied first), as antsApplyTransforms expects.",
      "required": true,
      "cli": {
        "argstr": "%s"
      },
      "items_type": "file"
    },
    "invert_transform_flags": {
      "type": "list",
      "items_type": "bool",
      "desc": "Whether to invert each transform, one logical per transform (linear transforms only)."
    },
    "print_out_composite_warp_file": {
      "type": "bool",
      "default": false,
      "desc": "Write the composition of transforms as a displacement field on the reference grid instead of a transformed image."
    }
  },
  "outputs": {
    "output_image": {
      "type": "file",
      "path": {
        "from_input": "output_image"
      },
      "must_exist": false
    }
  },
  "runtime": {
    "profile": "ants"
  },
  "origin": {
    "source": "nipype",
    "nipype_class": "nipype.interfaces.ants.resampling.ApplyTransforms",
    "nipype_version": "1.10.0"
  },
  "description": "ApplyTransforms, applied to an input image, transforms it according to a",
  "render": "ants_apply_transforms"
}
