% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_auto.R
\name{ni_afni_fwh_mx}
\alias{ni_afni_fwh_mx}
\title{AFNI FWHMx}
\usage{
ni_afni_fwh_mx(
  in_file,
  acf = FALSE,
  args = NULL,
  arith = NULL,
  automask = FALSE,
  combine = NULL,
  compat = NULL,
  demed = NULL,
  detrend = FALSE,
  geom = NULL,
  mask = NULL,
  out_detrend = NULL,
  out_file = NULL,
  out_subbricks = NULL,
  unif = NULL,
  .cwd = NULL,
  .env = NULL,
  .engine = NULL,
  .profile = NULL,
  dry_run = FALSE,
  echo = interactive()
)
}
\arguments{
\item{in_file}{Character; file path. input dataset \strong{Required.}}

\item{acf}{Character. computes the spatial autocorrelation}

\item{args}{Character. Additional parameters to the command}

\item{arith}{Logical. if in_file has more than one sub-brick, compute the final estimate as the arithmetic mean of the individual sub-brick FWHM estimates}

\item{automask}{Logical. compute a mask from THIS dataset, a la 3dAutomask}

\item{combine}{Logical. combine the final measurements along each axis}

\item{compat}{Logical. be compatible with the older 3dFWHM}

\item{demed}{Logical. If the input dataset has more than one sub-brick (e.g., has a time axis), then subtract the median of each voxel's time series before processing FWHM. This will tend to remove intrinsic spatial structure and leave behind the noise.}

\item{detrend}{Character. instead of demed (0th order detrending), detrend to the specified order. If order is not given, the program picks q=NT/30. -detrend disables -demed, and includes -unif.}

\item{geom}{Logical. if in_file has more than one sub-brick, compute the final estimate as the geometric mean of the individual sub-brick FWHM estimates}

\item{mask}{Character; file path. use only voxels that are nonzero in mask}

\item{out_detrend}{Character; file path. Save the detrended file into a dataset}

\item{out_file}{Character; file path. output file}

\item{out_subbricks}{Character; file path. output file listing the subbricks FWHM}

\item{unif}{Logical. If the input dataset has more than one sub-brick, then normalize each voxel's time series to have the same MAD before processing FWHM.}

\item{.cwd}{Working directory override.}

\item{.env}{Named character vector of environment variables.}

\item{.engine}{Execution engine override.}

\item{.profile}{Runtime profile override.}

\item{dry_run}{Logical; preview command without executing.}

\item{echo}{Logical; echo stdout/stderr in real time.}
}
\value{
An \code{ni_result} object.
}
\description{
Unlike the older 3dFWHM, this program computes FWHMs for all sub-bricks
}
