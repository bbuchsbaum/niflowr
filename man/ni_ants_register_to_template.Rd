% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ni_ants_template.R
\name{ni_ants_register_to_template}
\alias{ni_ants_register_to_template}
\title{Register a T1w image to a T1w template with a pinned preset}
\usage{
ni_ants_register_to_template(
  fixed_image,
  moving_image,
  output_prefix,
  preset = c("precise", "testing"),
  ...,
  .cwd = NULL,
  .env = NULL,
  .engine = NULL,
  .profile = NULL,
  timeout = Inf,
  dry_run = FALSE,
  echo = interactive()
)
}
\arguments{
\item{fixed_image}{The template (reference) image, usually brain-extracted.}

\item{moving_image}{The T1w image to register, brain-extracted like the
template.}

\item{output_prefix}{Path prefix for the transforms and the warped image
(\verb{<prefix>Warped.nii.gz}).}

\item{preset}{Preset name; see \code{\link[=ni_ants_template_preset]{ni_ants_template_preset()}}.}

\item{...}{Named \code{ants.registration} inputs that replace preset values
whole, e.g. \code{random_seed = 1} or \code{fixed_image_mask = "mask.nii.gz"}.
\code{NULL} removes a preset value. Related values are not adjusted: replacing
\code{metric} keeps the preset's \code{radius_or_number_of_bins}. Giving
\code{initial_moving_transform} drops the preset's
\code{initial_moving_transform_com}, since only one initialisation applies.}

\item{.cwd}{Working directory override.}

\item{.env}{Named character vector of environment variables.}

\item{.engine}{Execution engine override.}

\item{.profile}{Runtime profile override.}

\item{timeout}{Seconds before the registration is stopped.}

\item{dry_run}{Logical; preview command without executing.}

\item{echo}{Logical; echo stdout/stderr in real time.}
}
\value{
An \code{ni_result} (or an execution plan when \code{dry_run = TRUE}).
}
\description{
Runs \code{antsRegistration} (Rigid, Affine, then SyN) with a schedule from
\code{\link[=ni_ants_template_preset]{ni_ants_template_preset()}}, writing the moving image resampled into the
template and the ITK composite transforms (\verb{<prefix>Composite.h5} and
\verb{<prefix>InverseComposite.h5}), the form fMRIPrep writes.
}
\details{
This is a named preset, not the default of \code{\link[=ni_ants_registration]{ni_ants_registration()}}: the
\code{"precise"} schedule is tuned for about 1 mm T1w-to-template registration
and is the wrong schedule for other modalities or resolutions. A
registration can run, write a transform, and not fold while still stopping
at a poor optimum, so check it with \code{\link[=ni_ants_registration_qa]{ni_ants_registration_qa()}}.
}
\examples{
\dontrun{
reg <- ni_ants_register_to_template(
  fixed_image = "/data/tpl-MNI152NLin2009cAsym_res-01_desc-brain_T1w.nii.gz",
  moving_image = "/data/sub-01_desc-brain_T1w.nii.gz",
  output_prefix = "/data/out/sub-01_from-T1w_to-MNI_"
)
ni_ants_registration_qa(reg,
  fixed_mask = "/data/tpl-MNI152NLin2009cAsym_res-01_desc-brain_mask.nii.gz",
  moving_mask = "/data/sub-01_desc-brain_mask.nii.gz"
)
}
}
\seealso{
\code{\link[=ni_ants_registration_qa]{ni_ants_registration_qa()}}
}
