% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ni_ants_template.R
\name{ni_ants_registration_qa}
\alias{ni_ants_registration_qa}
\title{Measure how well a registration aligned}
\usage{
ni_ants_registration_qa(
  registration = NULL,
  fixed_image = NULL,
  moving_image = NULL,
  transform = NULL,
  fixed_mask = NULL,
  moving_mask = NULL,
  fixed_labels = NULL,
  moving_labels = NULL,
  similarity = c(MI = 32, CC = 4),
  interpolation = "Linear",
  jacobian = TRUE,
  keep_field = FALSE,
  out_dir = NULL,
  .engine = NULL,
  .profile = NULL,
  timeout = Inf,
  echo = FALSE
)
}
\arguments{
\item{registration}{An \code{ni_result} from \code{\link[=ni_ants_register_to_template]{ni_ants_register_to_template()}} or
\code{\link[=ni_ants_registration]{ni_ants_registration()}} run with \code{write_composite_transform = TRUE}.
Supplies the images, transform, engine, and profile not given explicitly.}

\item{fixed_image, moving_image}{The template and the registered image. For
multi-channel registrations the first channel is used.}

\item{transform}{The transform that resamples the moving image into template
space: a composite \code{.h5}, or several files in \code{antsApplyTransforms} order
(e.g. \code{c("x_1Warp.nii.gz", "x_0GenericAffine.mat")}).}

\item{fixed_mask, moving_mask}{Brain masks. \code{fixed_mask} limits the
similarity metrics and the Jacobian summary; with both, the warped moving
mask's Dice with \code{fixed_mask} is reported.}

\item{fixed_labels, moving_labels}{Label images; with both, per-label Dice
after warping \code{moving_labels} with \code{GenericLabel} interpolation.}

\item{similarity}{Named numeric vector of metrics to compute, each with its
bins (MI, Mattes) or radius (CC, others).}

\item{interpolation}{Interpolation for the warped moving image.}

\item{jacobian}{Whether to compute the Jacobian determinant summary.}

\item{keep_field}{Whether to keep the composed displacement field (large at
1 mm) after computing the Jacobian.}

\item{out_dir}{Directory in which each call creates its QA directory.
Defaults to the transform's directory; set it to keep QA files out of a
derivatives tree. Must be reachable by the engine (a mapped root for
containers).}

\item{.engine, .profile}{Execution engine and runtime profile; default to
the registration's.}

\item{timeout}{Seconds before each ANTs step is stopped.}

\item{echo}{Whether to echo tool output.}
}
\value{
An \code{ni_registration_qa} object: a list with \code{similarity} (data
frame), \code{mask_dice} (\code{NA} without both masks), \code{label_dice} (data frame,
or \code{NULL} without both label images), \code{jacobian} (summary list, or \code{NULL}
when \code{jacobian = FALSE}), and \code{files}.
}
\description{
A registration can run, write a transform, and not fold while stopping at a
poor optimum. This reports the evidence that separates the two: image
similarity between the template and the moving image warped into it,
overlap of warped masks or labels, and the Jacobian determinant of the
transform. All image work runs through ANTs (\code{antsApplyTransforms},
\code{MeasureImageSimilarity}, \code{CreateJacobianDeterminantImage}) on the same
engine as the registration.
}
\details{
The moving image is always re-warped here with \code{interpolation}, so scores
are comparable between registrations (for example niflowr's and fMRIPrep's
transforms) only with the same images, mask, interpolation, and metric
parameters. Similarity values are ANTs metric costs, so \strong{lower is better};
they are computed densely (no sampling), so they are deterministic. Dice is
overlap, so higher is better. A non-positive Jacobian determinant marks
folding.

Each call writes its images to a new directory inside \code{out_dir}, so QA can
be re-run. Relative paths are resolved against the R working directory.
}
\seealso{
\code{\link[=ni_ants_register_to_template]{ni_ants_register_to_template()}}
}
