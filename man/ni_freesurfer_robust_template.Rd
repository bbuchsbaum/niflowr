% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_auto.R
\name{ni_freesurfer_robust_template}
\alias{ni_freesurfer_robust_template}
\title{FREESURFER RobustTemplate}
\usage{
ni_freesurfer_robust_template(
  auto_detect_sensitivity,
  in_files,
  out_file,
  outlier_sensitivity,
  args = NULL,
  average_metric = NULL,
  fixed_timepoint = NULL,
  in_intensity_scales = NULL,
  initial_timepoint = NULL,
  initial_transforms = NULL,
  intensity_scaling = NULL,
  no_iteration = NULL,
  scaled_intensity_outputs = NULL,
  subsample_threshold = NULL,
  transform_outputs = NULL,
  .cwd = NULL,
  .env = NULL,
  .engine = NULL,
  .profile = NULL,
  dry_run = FALSE,
  echo = interactive()
)
}
\arguments{
\item{auto_detect_sensitivity}{Logical. auto-detect good sensitivity (recommended for head or full brain scans) \strong{Required.}}

\item{in_files}{Character or numeric vector. input movable volumes to be aligned to common mean/median template \strong{Required.}}

\item{out_file}{Character; file path. output template volume (final mean/median image) \strong{Required.}}

\item{outlier_sensitivity}{Numeric. set outlier sensitivity manually (e.g. "--sat 4.685" ). Higher values mean less sensitivity. \strong{Required.}}

\item{args}{Character. Additional parameters to the command}

\item{average_metric}{Character; one of: "median", "mean". construct template from: 0 Mean, 1 Median (default)}

\item{fixed_timepoint}{Logical. map everything to init TP# (init TP is not resampled)}

\item{in_intensity_scales}{Character or numeric vector. use initial intensity scales}

\item{initial_timepoint}{Integer. use TP# for special init (default random), 0: no init}

\item{initial_transforms}{Character or numeric vector. use initial transforms (lta) on source}

\item{intensity_scaling}{Logical. allow also intensity scaling (default off)}

\item{no_iteration}{Logical. do not iterate, just create first template}

\item{scaled_intensity_outputs}{Character or numeric vector. final intensity scales (will activate --iscale)}

\item{subsample_threshold}{Integer. subsample if dim > # on all axes (default no subs.)}

\item{transform_outputs}{Character or numeric vector. output xforms to template (for each input)}

\item{.cwd}{Working directory override.}

\item{.env}{Named character vector of environment variables.}

\item{.engine}{Execution engine override.}

\item{.profile}{Runtime profile override.}

\item{dry_run}{Logical; preview command without executing.}

\item{echo}{Logical; echo stdout/stderr in real time.}
}
\value{
An \code{ni_result} object.
}
\description{
construct an unbiased robust template for longitudinal volumes
}
