% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_auto.R
\name{ni_freesurfer_mr_is_expand}
\alias{ni_freesurfer_mr_is_expand}
\title{FREESURFER MRIsExpand}
\usage{
ni_freesurfer_mr_is_expand(
  distance,
  in_file,
  args = NULL,
  dt = NULL,
  nsurfaces = NULL,
  out_name = "expanded",
  pial = NULL,
  smooth_averages = NULL,
  spring = NULL,
  thickness = NULL,
  thickness_name = NULL,
  write_iterations = NULL,
  .cwd = NULL,
  .env = NULL,
  .engine = NULL,
  .profile = NULL,
  dry_run = FALSE,
  echo = interactive()
)
}
\arguments{
\item{distance}{Numeric. Distance in mm or fraction of cortical thickness \strong{Required.}}

\item{in_file}{Character; file path. Surface to expand \strong{Required.}}

\item{args}{Character. Additional parameters to the command}

\item{dt}{Numeric. dt (implicit: 0.25)}

\item{nsurfaces}{Integer. Number of surfacces to write during expansion}

\item{out_name}{Character. Output surface file. If no path, uses directory of \code{in_file}. If no path AND missing "lh." or "rh.", derive from \code{in_file}}

\item{pial}{Character. Name of pial file (implicit: "pial") If no path, uses directory of \code{in_file} If no path AND missing "lh." or "rh.", derive from \code{in_file}}

\item{smooth_averages}{Integer. Smooth surface with N iterations after expansion}

\item{spring}{Numeric. Spring term (implicit: 0.05)}

\item{thickness}{Logical. Expand by fraction of cortical thickness, not mm}

\item{thickness_name}{Character. Name of thickness file (implicit: "thickness") If no path, uses directory of \code{in_file} If no path AND missing "lh." or "rh.", derive from \code{in_file}}

\item{write_iterations}{Integer. Write snapshots of expansion every N iterations}

\item{.cwd}{Working directory override.}

\item{.env}{Named character vector of environment variables.}

\item{.engine}{Execution engine override.}

\item{.profile}{Runtime profile override.}

\item{dry_run}{Logical; preview command without executing.}

\item{echo}{Logical; echo stdout/stderr in real time.}
}
\value{
An \code{ni_result} object.
}
\description{
Expands a surface (typically ?h.white) outwards while maintaining
}
