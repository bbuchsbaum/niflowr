% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ni_openneuro.R
\name{ni_from_openneuro}
\alias{ni_from_openneuro}
\title{Fetch and prepare OpenNeuro dataset}
\usage{
ni_from_openneuro(
  dataset_id,
  tag = NULL,
  subjects = NULL,
  spec_id = NULL,
  modality = NULL,
  subid = NULL,
  task = NULL,
  session = NULL,
  run = NULL,
  derivatives = FALSE,
  prep_dir = "derivatives/fmriprep",
  quiet = FALSE,
  force = FALSE,
  ...
)
}
\arguments{
\item{dataset_id}{OpenNeuro dataset identifier (e.g. \code{"ds000114"}).}

\item{tag}{Optional dataset version tag (e.g. \code{"1.0.0"}).}

\item{subjects}{Optional subject IDs to download (default: all subjects).}

\item{spec_id}{Spec ID or \code{ni_spec} object to match inputs against (optional).}

\item{modality}{Image modality to search for (e.g. \code{"T1w"}, \code{"bold"}).}

\item{subid}{Subject ID filter for input query.}

\item{task}{Task filter for input query.}

\item{session}{Session filter for input query.}

\item{run}{Run filter for input query.}

\item{derivatives}{Include derivatives in the BIDS project.}

\item{prep_dir}{Path to preprocessed derivatives directory.}

\item{quiet}{Suppress progress messages.}

\item{force}{Force re-download of cached data.}

\item{...}{Additional arguments passed to \code{openneuroR::on_fetch()}.}
}
\value{
If \code{spec_id} or \code{modality} is provided, returns a data.frame from
\code{ni_bids_inputs()}. Otherwise, returns a \code{bids_project} object.
}
\description{
Downloads an OpenNeuro dataset and optionally queries it for BIDS inputs
matching a spec. This is a convenience wrapper around the openneuroR workflow.
}
\examples{
\dontrun{
# Get the full BIDS project
proj <- ni_from_openneuro("ds000114")

# Get specific inputs for a spec
inputs <- ni_from_openneuro("ds000114", spec_id = "fsl_feat", modality = "bold")
}
}
